return {
   menu       = require(... .. '.menu'),
   wibar      = require(... .. '.wibar'),
   calendar   = require(... .. '.calendar'),
   exitscreen = require(... .. '.exitscreen.init'),
   popups     = require(... .. '.popups')
}
